```
.
├── compare.py      # compare our framework with pytorch
├── modules.py      # implementation of the modules
├── optim.py        # implementation of the SGD optimizer
├── report.pdf      # report of the project
├── test.py         # creates dataset, network, trains with SGD 
└── torch_test.py   # does the same as test but using PyTorch (for comparison)
```
